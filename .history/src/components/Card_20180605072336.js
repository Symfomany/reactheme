import React, { Component } from "react";
import { Card as CardStyled, CardTitle, CardContent } from "../styled/Card";

class Card extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return (
     
    );
  }
}

export default Card;
